# Table/Figure → Script Crosswalk

> **Draft.** Sections marked ⚠️ need your confirmation.

## Figures 2–6 — caption fix needed (validation, not testing, error)

Figures 2, 3, 4, 5, and 6 are currently captioned "testing error/loss," but this was a proofreading inconsistency — they should say **validation error**, matching both referee comment 2.6.5's suggestion and what the ADAM solver's per-iteration history (`his.his`) actually logs (train + validation only; test is a final scalar, not tracked per iteration). No code or data change needed — just fix the caption text and any in-plot legend labels reading "testing" in `main_new.tex`.

## Table 2 — relative error by architecture/depth/optimizer

**Confirmed** (Leapfrog columns removed, CDR already shows relative-error values in the latest PDF).

Generated by: `run_all_table2.m` → `collectResults.m` → `results/table2_pivot.csv`

## Table 3 — DCR Hamiltonian, ΔError vs. Legendre degree (3–6)

⚠️ Same degree-3–6 sweep as Figures 4–5 below, sliced to DCR/Hamiltonian/ADAM only. Still need to confirm **which `T`** — no `T` is mentioned in the caption, and every `runConfigBatch` group includes the full degree sweep regardless of `T`. Best guess is `T=1` (matching the convention used in Figures 2/3, Table 5, and Figure 6), but please confirm.

## Figures 2–3 — training/validation error curves (ResNet, Hamiltonian; ADAM; monomial vs. Legendre)

Generated by: `collectResults.m`'s tikz export (ADAM/sgd, `T=1` runs) → `results/tikzdata/*.dat`

## Figures 4–5 — validation error convergence across Legendre degree 3–6 (ResNet, Hamiltonian; all 3 datasets)

Same degree-3–6 sweep as Table 3, spanning all three datasets and both architectures (not just DCR/Hamiltonian). Generated the same way as Figures 2–3, filtered to the degree-sweep configs.

## Figure 6 — neural ODE loss vs. function evaluations

Python pipeline (`ode_demo_NEW.py` / `timeparam_NEW.py`). Caption already says "train/validation," so likely unaffected by the testing-vs-validation issue above — worth a quick double-check once we're through this.

## Table 4 — neural ODE parameter count and test error by degree

Python pipeline, degree-sweep script (`run_table4_degrees.sh`).

## Tables 5–6 — neural ODE train/val/test error, stationary ODE task

Python pipeline.
