# Table/Figure Reproduction Overview

## Figures 2–6 — caption fix needed (validation, not testing, error)

**Explanation needed here.** Captions fixed.

## Table 2 — relative error by architecture/depth/optimizer

Generated by: `run_all_table2.m` → `collectResults.m` → `results/table2_pivot.csv`

## Table 3 — DCR Hamiltonian, ΔError vs. Legendre degree (3–6)

(!!!) Same degree-3–6 sweep as Figures 4–5 below, sliced to DCR/Hamiltonian/ADAM only. Still need to mention **`T=1`** — no `T` in the caption, note every `runConfigBatch` group includes the full degree sweep regardless of `T`. 

## Figures 2–3 — training/validation error curves (ResNet, Hamiltonian; ADAM; monomial vs. Legendre)

Generated by: `collectResults.m`'s tikz export (ADAM/sgd, `T=1` runs) → `results/tikzdata/*.dat`

## Figures 4–5 — validation error convergence across Legendre degree 3–6 (ResNet, Hamiltonian; all 3 datasets)

Same degree-3–6 sweep as Table 3, spanning all three datasets and both architectures (not just DCR/Hamiltonian). Generated the same way as Figures 2–3, filtered to the degree-sweep configs.

## Figure 6 — neural ODE loss vs. function evaluations

Python folder? (`ode_demo_NEW.py` / `timeparam_NEW.py`). 

## Table 4 — neural ODE parameter count and test error by degree

Python folder? degree-sweep script (`run_table4_degrees.sh`).

## Tables 5–6 — neural ODE train/val/test error, stationary ODE task

Python pipeline add details?
