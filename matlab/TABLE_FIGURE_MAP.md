# Table/Figure → Script Crosswalk

> **Draft.** Sections marked ⚠️ need your confirmation before this is trustworthy for a reviewer.

## Table 2 — testing relative error by architecture/depth/optimizer

**Generated by:** `experiments/run_all_table2.m` (all 18 `(dataset, T, opti)` groups) → `experiments/collectResults.m` → `results/table2_pivot.csv`

Covers ResNet and Hamiltonian architectures only (Leapfrog excluded — see Section 5.2 of the manuscript). Each cell is `mean(relErrTest)` from the corresponding `results/{dataset}_{dynamic}_{basis}_d{d}_T{T}_{opti}.mat` file.

**Run command:**
```matlab
run_all_table2
```

## Table 3 — DCR Hamiltonian, ΔError vs. Legendre degree (3–6)

**Generated by:** ⚠️ *Believed to be a subset of the same `T=1, sgd, DCR` sweep from Table 2* — `runConfigBatch`'s config list already includes Legendre degrees 3–6 and the non-parameterized baseline for both `ResNN` and `hamiltonian`, so Table 3 looks like it's just the DCR/Hamiltonian/ADAM rows of that same sweep, sliced by degree instead of by architecture/optimizer. **Please confirm this is right** — if Table 3 was actually generated by a separate run (e.g., a different `T`, or GNvpro rather than ADAM), let me know and I'll correct this section.

## Figures 2–3 — training/testing error curves (ResNet, Hamiltonian)

**Generated by:** `collectResults.m`'s automatic tikz export (ADAM/sgd, `T=1` runs only) → `results/tikzdata/*.dat`

## Figures 4–5 — convergence across Legendre degree

⚠️ *Not yet mapped.* These need the degree-3–6 sweep's per-iteration history, not just the final relative error — need to confirm whether `collectResults.m`'s tikz export already covers this or whether a separate export step is needed.

## Figure 6 — neural ODE loss vs. function evaluations

**Generated by:** Python pipeline (`ode_demo_NEW.py` / `timeparam_NEW.py`) — not yet in this repo.

## Table 4 — neural ODE parameter count and test error by degree

**Generated by:** Python pipeline, degree-sweep script (`run_table4_degrees.sh`) — not yet in this repo.

## Tables 5–6 — neural ODE train/val/test error, stationary ODE task

**Generated by:** Python pipeline — not yet in this repo.
